(* 错误类型：语义错误 - 递归调用无限循环 *)
MODULE Fibonacci;

VAR
  n, result: INTEGER;

PROCEDURE Fib(x: INTEGER): INTEGER;
BEGIN
  IF x <= 1 THEN
    RETURN x
  ELSE
    RETURN Fib(x) + Fib(x - 2) (* 递归调用 Fib(x) 导致无限循环 *)
  END
END Fib;

BEGIN
  n := 6;
  result := Fib(n)
END Fibonacci.