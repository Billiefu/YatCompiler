(* 错误类型：词法错误 - 非法标识符（包含数字开头） *)
MODULE Fibonacci;

VAR
  n, result: INTEGER;

PROCEDURE Fib(x: INTEGER): INTEGER;
BEGIN
  IF x <= 1 THEN
    RETURN x
  ELSE
    RETURN Fib(x - 1) + Fib(x - 2)
  END
END Fib;

BEGIN
  n := 6;
  result := Fib(n)
END 1Fibonacci. (* 非法模块名：数字开头 *)
